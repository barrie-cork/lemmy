{
  "id": 284,
  "from": "impl",
  "kind": "blocker",
  "timestamp": "2026-05-19T19:45:00Z",
  "question": "fix-impl-2 cross-platform dead_code discrepancy: laptop clippy saw 6 dead_code items in inbox.rs (trait GovernanceInboundActivity + rate_per_peer_counts + current_hour_bucket + log_inbox_drop + rate_per_actor_counts + wrap_governance_inbound); worker clippy sees only 2 (rate_per_actor_counts + wrap_governance_inbound). The 4 that differ are referenced inside wrap_governance_inbound's body — Rust's dead_code analysis on Linux flags only the root uncalled function (wrap_governance_inbound), not its transitive callees. Option A: advisor re-runs §15 cargo-clippy on laptop from a COLD build (no cached artifacts) — if laptop now also shows only 2 dead_code items (consistent with worker), no further fix needed and Task 4 §15 green. Option B: laptop genuinely shows 4 extra dead_code items even on cold rebuild — need a different suppression strategy since allow_attributes=deny in workspace and #[allow] is blocked.",
  "options": [
    "option-a: advisor re-runs §15 cargo-clippy from cold build on laptop; if laptop agrees only 2 are dead_code, accept the committed 3-edit fix as complete for fix-impl-2",
    "option-b: laptop cold-rebuild still shows 4 extra dead_code — advisor authors fix-impl-2b with alternative approach (e.g. refactor to move callers into a live function or use feature flag gating)"
  ],
  "context": "Worker pre-push gates: FI2_CLIPPY_EXIT_0 (v2 log at .claude/PRPs/debug/fed-in-b-fi2-clippy-v2.log), FI2_CHECK_EXIT_0 (v2 log at .claude/PRPs/debug/fed-in-b-fi2-check-v2.log). Committed 3 edits to crates/apub/activities/src/governance/inbox.rs: (1) #[expect(dead_code, reason=...)] on rate_per_actor_counts (~L470 — zero callers anywhere), (2) #[expect(dead_code, reason=...)] on wrap_governance_inbound (~L485 — zero callers anywhere), (3) .unwrap_or_else(std::sync::PoisonError::into_inner) @~L548 (redundant closure fix). The 4 items attempted but reverted because #[expect(dead_code)] on them caused unfulfilled_lint_expectations under -D warnings: GovernanceInboundActivity trait (referenced as trait bound A: GovernanceInboundActivity in wrap_governance_inbound's where clause), rate_per_peer_counts (called at ~L549 inside wrap_governance_inbound body), current_hour_bucket (called at ~L547 inside wrap_governance_inbound body), log_inbox_drop (called 4× at ~L516/531/558/587 inside wrap_governance_inbound body). Root: Rust dead_code lint fires only on the ROOT uncalled item in a call chain, not transitive callees. wrap_governance_inbound is the root; its body calls the others, making them non-dead per the Linux worker's analysis. Whether the Windows laptop's clippy treats these 4 consistently is unknown — the advisor's §15 reval at cdff6f09d saw all 6 as dead_code, but that may have been influenced by stale cached build artifacts. Brief §4.5 capped at 7 named edits; 8th edit beyond those would require a fix-impl-2b (different approach). workspace allow_attributes=deny means #[allow(dead_code)] is also blocked.",
  "answer": null,
  "answered_by": null,
  "resolved_at": null
}
