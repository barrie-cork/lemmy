# No Destructive Defaults

- Never use `--force`, `--no-verify`, or `--hard` flags without explicit user request
- Never delete files or directories without confirmation
- Never overwrite uncommitted changes without warning
- Investigate before overwriting — check git status, read existing content
- If a safety check fails, fix the underlying issue rather than bypassing the check
