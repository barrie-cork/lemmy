<!--
Baseline iteration 1: empty. Relies on CLAUDE.md + .claude/rules/*.md + agent
frontmatter (.claude/agents/bm-task.md) for behavioural context. Future
iterations may append a role-specific system prompt here once RLS signals
identify the strip surface; the file is wired into the eventual `claude -p
--bare --append-system-prompt` dispatch (Task 5 — deferred). See README.md.
-->
