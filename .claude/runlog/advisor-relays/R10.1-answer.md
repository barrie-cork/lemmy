---
id: R10.1-answer
from: advisor
to: impl
ts: 2026-04-23T23:40Z
relates_to: plan-phase-v1-JM-a-task-10
decision: c
---

# Decision
Extend `PHASE_1_MIGRATION_COUNT` 9 → 12. No DQ filing. `answered_by: user`.

# Instructions
1. Set `const PHASE_1_MIGRATION_COUNT: u64 = 12;` in `crates/server/tests/e2e.rs`.
2. Rewrite comment at `crates/server/tests/e2e.rs:311-321`. Current comment claims "6 Phase1 + 2 Phase5a + 1 Phase5b = 9" but LIFO revert of 9 actually includes `governance_log_notify` pair + `federation_attestations`. Comment must acknowledge LIFO-positional semantics, not semantic-set fiction.
3. Add inline `TODO(v0-polish):` at the constant citing two items:
   - GH #43 (existing deflake reason in `#[ignore]` attribute)
   - NEW GH issue sketch (file in Task 11 retro): `PHASE_1_MIGRATION_COUNT` is a LIFO count not a semantic set; post-trunk migrations added after last bump silently take Nth slot; v1-AD-a's 4 migrations currently uncounted and would be silently swapped into revert list on un-ignore.
4. Do NOT extend by 7. AD-a retrofit out-of-scope per plan §18 row 7 + §19 Notes. Future retrofit = separate `chore(test): retrofit v1-AD-a migrations into phase1_migrations_round_trip` commit.

# Retro carry (Task 11)
- R10.1 root cause: plan §13 Task 10 anticipated single-number drift; reality is semantic-model bug.
- File GH issue sketch for count-model semantic mismatch.
- Recommend named-migration list replacing count-based revert for future phases.

# Next
Proceed with rest of Task 10: table-list loop extensions + new `v1_jm_a_backfill_populates_v0_snapshot` test.
