# Auto-phase handover — test — impl-cohort-1-task1-running

**Generated:** 2026-06-12T00:05Z  
**Sub-phase:** test (dogfood sandbox)  
**Stage:** impl-cohort-1-running → task-1 impl dispatched  
**Last commit (governance-v0):** `624a2132b` chore(advisor): impl-task briefs test task-0 + task-1 — sandbox_clamp dogfood  
**Phase branch tip (phase-test):** `0a745c56d` chore: sync governance-v0 into phase-test (plan + impl-task briefs)

## RESUME — next action (re-verify on resume)

> **HYPOTHESIS — verify before acting:** Task-1 impl Junior #655 running (or done). Poll `list_tasks` for status.

- On `done` → read DQ for `validate-pending` entry from #655 → get `workflow_run_id` → dispatch ci-watcher
- On `failed` → read task logs, classify error, apply §G4 (if non-allowlist → catch-fire)
- On still `running` → wait, re-poll in 5 min

### Expected DQ after #655 completes

Worker should push a `kind: "validate-pending"` entry with:
- `workflow_run_id`: integer (or 0 if no GH Actions run due to low credits)
- `branch`: worker branch name (e.g. `junior/role-impl-task-test-task-1-*`)
- `phase_task`: 1
- `result`: null (ci-watcher will mutate)

If `workflow_run_id == 0` (no GH Actions run triggered): advisor §G4 classifies as `run_not_found` → catch-fire to user (non-allowlist).

### After ci-watcher pass

Next stage: `bm-pr` — dispatch `[role:bm-task] test bm-pr — see .claude/PRPs/briefs/test-bm-pr-N.md` (brief not yet authored; author after ci-watcher confirms pass).

## Stage digest (recent, last 4)

1. `bm-cut-running` (20:27Z): bm-cut #652 queued; phase-test created
2. `planning-running` (20:34Z): planning #653 done in ~7 min; plan approved (gate 1, Rust+Shape G); briefs authored
3. `impl-cohort-1-running` (21:20Z): task-0 #654 queued; task-1 brief ready
4. `impl-cohort-1-task1-running` (00:05Z): task-0 #654 PROBES:ALL PASS; task-1 #655 queued

## Plan shape

- Task 0: pre-flight audit — **#654 done ✓ (PROBES:ALL PASS)**
- Task 1: `sandbox_clamp` impl + Shape G push-and-exit + `validate-pending` DQ — **#655 running**
- Task 2: retro (post-merge, advisor-authored)
- No `[P]` markers — serial dispatch

## Known non-gating notes

- **GH Actions credits may be low** (user note 2026-06-11): task-1 brief §5 instructs worker to set `workflow_run_id=0` if no run appears; ci-watcher classifies `run_not_found`; advisor handles via §G4
- Telegram completion hook absent (daemon list_hooks = empty after restart); poll `list_tasks` instead
- pmd-http-mcp laptop NSSM service still running stale DB; workers use homeserver stdio PMD
- Task-0 submodule note: worker ran `git submodule update --init --recursive` (was uninitialised in worktree); subsequent task-1 worker on phase-test should not need this (submodule init is per-worktree, not inherited)
