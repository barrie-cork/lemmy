---
phase: m2-late-2
stage: impl-cohort-0-running
authored: 2026-06-12
authored_by: advisor (canonical brehon-fork / governance-v0 session)
purpose: Auto-handover written at impl-cohort-0 dispatch. Resumer continues at impl-cohort-0 polling loop.
lane_mode: B
---

# ⏩ RESUME — m2-late-2 impl-cohort-0 running

**You are the advisor for Brehon m2-late-2** (`--unattended` mode). Task #661 (Task 0 pre-flight) was
dispatched at 2026-06-12 ~13:05 UTC. Resume by invoking:

```
/auto-phase m2-late-2 --unattended --start-from impl-cohort-0
```

The skill's Phase 0.5 will reconcile Junior task #661 status and route accordingly.

## Current state (VERIFIED_AT: 266a2dc94)

- **governance-v0 HEAD:** `af8ba7ccb` (post compaction; run `git rev-parse --short governance-v0` to verify current)
- **Phase branch tip (origin):** `266a2dc94` (last known; run `git ls-remote origin phase-m2-late-2` to confirm)
- **Stage:** `impl-cohort-0-running`
- **Junior task #661:** queued/running — `[role:impl-task] m2-late-2 task-0 pre-flight`
- **DQ pending:** 0
- **Gate 1 (plan-approval):** CLEARED (prior session)
- **Gate 4 (e2e mode):** will auto-set to `local` on first Phase 2 encounter (--unattended)

## What #661 (Task 0) does

Runs probes -1 (submodule), 0 (Docker), 1 (branch), 2 (m2-late-1 base intact), 3-5 SKIPPED (NO-CARGO-ON-ELITEDESK),
6 (concurrent PR check). **No commit. No DQ unless probe fails.**

Expected output: `PROBES: ALL PASS` with 6 probe lines.

Expected duration: ~2 min.

## On Task 0 completion

- **ALL PASS** → advance to Cohort 1 dispatch: author T1[P] + T3[P] briefs, dispatch simultaneously (cap: 2 total).
  - T1[P]: `crates/api/api/src/governance/sanction_publisher.rs` (add case_id to SanctionEvent) + `crates/server/tests/e2e/m2_late.rs` (assert case_id in e2e)
  - T3[P]: `services/bridge/src/sanction_handler.rs` (read case_id from payload) + `services/bridge/src/bridge_room.rs` (add `lookup_by_case`)
- **PROBE FAIL** → catch-fire: surface probe failure + DQ blocker id to user. Do NOT auto-advance.

## Cohort plan (from m2-late-2.plan.md §13)

| Task | [P]? | Files | Notes |
|---|---|---|---|
| T0 | no | none | pre-flight; dispatched as #661 |
| T1 | [P] | sanction_publisher.rs, e2e/m2_late.rs | case_id payload + e2e assert |
| T3 | [P] | sanction_handler.rs, bridge_room.rs | bridge case_id mirror + lookup_by_case |
| T2 | no | sanction_publisher.rs | CR-A atomicity fix (run_transaction wrapping T1 writes) |
| T4 | [P] | sanction_handler.rs | bridge power-level enforcement (lookup_by_case → GET/merge/PUT) |
| T5 | no | bridge tests | bridge unit tests |
| T6 | manual | none | operator gate: verify pilot sanction_subscriber row |
| T7 | no | retro | phase retro |

T1 + T3 = Cohort 1 (dispatch simultaneously after T0 ALL PASS).
T2 + T4 = Cohort 2 (after Cohort 1 all done + validated).
T5 after Cohort 2.

## Critical rules (catch-fire on violation)

- **NO-CARGO-ON-ELITEDESK** — workers write `validate-pending-laptop` DQ and STOP
- **Bridge cargo LINUX-ONLY** — `scripts/brehon/cargo-linux.sh check --manifest-path services/bridge/Cargo.toml` (Docker); NEVER `cd services/bridge && cargo check` on Windows (ruma-common E0119)
- **R8**: `enqueue_sanction_event` uses FRESH transaction, NEVER touches vote-tx conn
- **R9**: `services/bridge` stays in root `Cargo.toml` `exclude`; NEVER `--workspace` for bridge
- **B-actor (portable-ID linkage) OUT OF SCOPE** — user-confirmed 2026-06-07

## Auto-state file

`.claude/auto-state/m2-late-2.json` — stage: `impl-cohort-0-running`, task 661 in `current_cohort.members[0]`.
