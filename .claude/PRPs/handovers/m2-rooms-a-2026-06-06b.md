# m2-rooms-a advisor handover — 2026-06-06 (session b)

**VERIFIED_AT: `45506e177` on `governance-v0`; phase branch `56de6da77` on `phase-m2-rooms-a`**

> Supersedes `.claude/PRPs/handovers/m2-rooms-a-2026-06-06.md` (session a). That
> handover said "next = author T2 brief". **T2 is now BLOCKED** — see RESUME.

## RESUME

**Current sub-phase:** `m2-rooms-a` — bridge-side room provisioning + hash-chain emission (M2-core Phases 3–5).

**State-machine stage:** T1 COMPLETE → **T2 BLOCKED on a planner DQ**. Do NOT author the T2 brief until the blocker is resolved.

**The blocker:** DQ `a3d0e9941441-055` (`kind: blocker`, `from: advisor`, pending). Plan §13 T2 requires the bridge to invite "the 5 assigned jurors as `Juror-<suffix>`", but the `CaseTransitionEvent` the bridge receives (`crates/api/api_common/src/governance.rs:860-866`) carries only `case_id`/`old_status`/`new_status`/`community_id`/`target_type` — **no juror identities** — and the bridge has no DB access. The headline acceptance criterion is unimplementable as scoped. Surfaced to the planner per user direction (do not decide the cross-toolchain seam in a brief).

**Next concrete action (when the planner answers DQ `-055`):**
1. Read the planner's answer in `.claude/decision-queue.json` resolved[] (entry `a3d0e9941441-055`).
2. If the answer re-scopes T2 (e.g. option-a augments `CaseTransitionEvent` + `bridge_notify.rs`), the planner should have amended `m2-rooms-a.plan.md` §13 T2 + §5.1/5.2 (complexity/files) + §11. Verify the plan was amended, not just the DQ answered.
3. Author `.claude/PRPs/briefs/m2-rooms-a-impl-2.md` against the re-scoped T2, Mode B sync onto `phase-m2-rooms-a`, dispatch `[role:impl-task]` with `base_branch: phase-m2-rooms-a`.

**If the planner's answer is option-a (augment the event):** T2 will then touch workspace files (`crates/api/api_common/src/governance.rs`, `crates/api/api_utils/src/bridge_notify.rs`) — its validation flips from `cd services/bridge && cargo check` to `./scripts/brehon/cargo-check.sh --workspace --features full`, and the §2.4 mandatory file-class lesson injection must re-run against the new workspace file list.

**Cross-session deps:**
- DQ pending[]: **1** — `a3d0e9941441-055` (the T2 blocker, awaiting planner).
- No concurrent Junior tasks running.
- governance-v0 tip: `45506e177` (synced laptop ↔ origin).

**Tripwires:**
- rusqlite pin in `services/bridge/Cargo.toml` is `"0.37"` (NOT the plan's advisory `"0.32"`) — already fixed on the phase branch at `56de6da77`. T2+ briefs must NOT re-pin.
- Telegram completion hook is **broken on the daemon** (`ModuleNotFoundError`). I removed hook ID 3; recreation via `create_hook` failed ("Junior MCP not configured"). **Manual polling required** until the daemon dependency is fixed. Re-check `list_hooks` at session start.
- `services/bridge/src/bridge_room.rs` shows untracked on `governance-v0` — that's correct, it's a phase-branch artifact (committed on `phase-m2-rooms-a`).
- The `.claude/PRPs/debug/*.json` + `*.py` untracked scratch files are pre-existing (carried since before this session); leave them.

---

## Git state snapshot

| Location | Branch | Tip SHA | Notes |
|---|---|---|---|
| laptop (`brehon-fork`) | `governance-v0` | `45506e177` | clean (only pre-existing untracked scratch) |
| origin | `governance-v0` | `45506e177` | synced |
| origin | `phase-m2-rooms-a` | `56de6da77` | T1 done + rusqlite-0.37 fix |
| daemon (`/srv/brehon-fork`) | `phase-m2-rooms-a` | `56de6da77` | checked out here after bm-cut finalize |

---

## What was done this session (session b)

1. **Resumed** from session-a handover; ran the 4 ASSUMES/VERIFY checks — all passed (phase tip `56de6da77`, daemon on `phase-m2-rooms-a`, DQ pending 0, no running tasks).
2. **Telegram hook** — found hook ID 3 with `ModuleNotFoundError`; removed it; recreation failed ("Junior MCP not configured"). Logged as a known daemon-side issue; manual polling for now.
3. **Read T2 plan spec + MIRROR refs** to author the brief — and found the juror-sourcing gap (see RESUME). Grounded it against the M1 `relay.rs` precedent (binary pushes `brehon_sender`/`brehon_recipient` into the notify payload; bridge never GETs identities back).
4. **User gate** (AskUserQuestion → "Surface to planner first").
5. **Filed DQ `a3d0e9941441-055`** (blocker to planner) on `governance-v0` via the atomic read-mutate-commit protocol. Committed `96f06bf15`, pushed, verified entry survived in committed HEAD.
6. **Retro** — eval ID 843 (score 0.78, root cause: missing-verification — proposed a fix before tracing the data-flow precedent).
7. **Promoted lesson** `feedback_trace_data_flow_precedent_before_proposing_seam.md` (commits `84d908609` + frontmatter fix `45506e177`); confirmed live in PMD (memory ID 844, queryable).

---

## Plan task status

| Task | Status | Notes |
|---|---|---|
| T0 (probes) | ✅ done | all 7 probes passed (session a) |
| T1 (config+store) | ✅ done | `8c6fb7366` + fix `56de6da77`; rusqlite 0.37; bridge_room.rs |
| T2 (room_provisioner core) | ⛔ BLOCKED | DQ `a3d0e9941441-055` — juror-sourcing seam undecided |
| T3–T6 | — | downstream of T2 |
| T7 (retro) | — | phase-end |

---

## ASSUMES / VERIFY before acting (next session)

| Assumption | How to verify (≤5 min each) |
|---|---|
| DQ `-055` still pending (not yet answered) | `ssh homeserver 'python3 -c "import io,json; d=json.load(io.open(\"/srv/brehon-fork/.claude/decision-queue.json\",encoding=\"utf-8\")); print([e[\"id\"] for e in d[\"pending\"]])"'` |
| If answered: plan §13 T2 was amended (not just DQ) | `git -C C:/Users/barri/Developer/brehon-fork log --oneline -- .claude/PRPs/plans/m2-rooms-a.plan.md \| head -3` |
| Daemon still on `phase-m2-rooms-a` | `ssh homeserver 'cd /srv/brehon-fork && git branch --show-current'` |
| phase-m2-rooms-a tip is `56de6da77` | `git ls-remote origin phase-m2-rooms-a` |
| Telegram hook status | `mcp__junior-brehon__list_hooks` (expect empty/broken until daemon dep fixed) |
