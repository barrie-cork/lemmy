# m2-rooms-a advisor handover — 2026-06-06

**VERIFIED_AT: `56de6da77` on `phase-m2-rooms-a`**

## RESUME

**Current sub-phase:** `m2-rooms-a` — bridge-side room provisioning + hash-chain emission (M2-core Phases 3–5).

**State-machine stage:** T1 COMPLETE → next = T2 dispatch.

**Last commit on phase branch:** `56de6da77` — `fix(bridge): bump rusqlite 0.32→0.37 + resolve DQ 7d593d3bc09f-001 pass`

**Next concrete action:** author `.claude/PRPs/briefs/m2-rooms-a-impl-2.md` (T2: room_provisioner.rs + appservice.rs), sync to phase branch (Mode B), dispatch `[role:impl-task]` with `base_branch: phase-m2-rooms-a`.

**Cross-session deps:**
- DQ pending[]: **empty** (all cleared)
- No concurrent Junior tasks running at handover time
- governance-v0 tip: `174da3093` (clean, no uncommitted state)

**Tripwires:**
- rusqlite pin in `services/bridge/Cargo.toml` is now `"0.37"` (not the plan's advisory `"0.32"`). T2+ briefs must NOT re-pin to 0.32 — the fix is already in the phase branch.
- Daemon is checked out on `phase-m2-rooms-a` (normal after bm-cut finalize). Mode B: impl-task briefs authored on governance-v0 → single-file checkout onto phase branch → `git push origin phase-m2-rooms-a` from daemon.
- Hook ID for Telegram completion: check `mcp__junior-brehon__list_hooks` at session start — was broken (ModuleNotFound) in this session; recreate if absent.

---

## Git state snapshot

| Location | Branch | Tip SHA | Notes |
|---|---|---|---|
| laptop (`brehon-fork`) | `governance-v0` | `174da3093` | clean working tree (bridge_room.rs untracked — that's fine, it's on the phase branch) |
| origin | `governance-v0` | `174da3093` | synced |
| origin | `phase-m2-rooms-a` | `56de6da77` | T1 done + fix |
| daemon (`/srv/brehon-fork`) | `phase-m2-rooms-a` | `56de6da77` | checked out here after bm-cut finalize |

---

## What was done this session

1. **Resumed from context boundary** — prior session had dispatched planning task #619.
2. **#619 done** — plan `m2-rooms-a.plan.md` (1140 lines) merged at `4aea30eec`. Pushed from daemon to origin, pulled to laptop.
3. **§3.4 DoD smoke** — all 7 Task 0 probes passed against HEAD.
4. **§3.5 watchpoint gate** — all 8 guardrails (R1–R8) cite specific files/commands. PASS.
5. **§3.5a MiniMax designation** — T3/T5/T6 qualify (3 new rows); cumulative 11/5+. Trial SUSPENDED (infra failures from m1-b; resume on user request).
6. **Gate 1 (plan approval)** — user approved.
7. **/precheck** — PASS (Tailscale up, daemon active, all patches applied, gov-v0 SYNC, not in forbidden window, 10 GB available).
8. **bm-cut brief authored** → committed `4707be299` → pushed → daemon synced → **task #620 dispatched** → done in 97s. Branch `phase-m2-rooms-a` live at `73041804a`.
9. **T0+T1 brief authored** (`m2-rooms-a-impl-1.md`) → committed `174da3093` on governance-v0 → Mode B sync onto phase branch (`3352af5af`) → **task #621 dispatched** → done in 3 min.
10. **validate-pending-laptop run** — `cd services/bridge && cargo check` failed first attempt: `libsqlite3-sys` conflict (`rusqlite 0.32` vs `matrix-sdk-sqlite`'s transitive `rusqlite 0.37`). Fixed: bumped to `"0.37"`. Exit 0 on retry. WP-6: 0.
11. **DQ `7d593d3bc09f-001`** mutated → `result: "pass"`, `answered_by: "advisor-laptop"`, moved to resolved[]. Committed as `56de6da77` on phase-m2-rooms-a.

---

## Plan task status

| Task | Status | Commit | Notes |
|---|---|---|---|
| T0 (probes) | ✅ done | (no commit) | all 7 probes passed |
| T1 (config+store) | ✅ done | `8c6fb7366` + fix `56de6da77` | rusqlite 0.37; BridgeConfig +3 fields; bridge_room.rs |
| T2 (room_provisioner core) | ⏳ next | — | C2.1 jury path + idempotency |
| T3 (C2.2–C2.6 + OQ-009) | — | — | requires T2 |
| T4a (binary room-event route) | — | — | requires T1 logic |
| T4b (binary bridge-read route) | — | — | requires T4a |
| T5 (callback wiring + soft_pause fix) | — | — | requires T2, T4a, T4b |
| T6 (integration tests) | — | — | requires T3, T5 |

---

## Key impl facts for T2 brief author

- **T2 files:** `services/bridge/src/room_provisioner.rs` (NEW), `services/bridge/src/appservice.rs` (extend), `services/bridge/src/main.rs` (add `mod room_provisioner;`). 3 files, 1 crate.
- **MIRROR refs:** `services/bridge/src/appservice.rs:144-191` (soft-pause gate + dispatch pattern); `services/bridge/src/provision.rs` (`create_community_room`); `services/bridge/src/puppet.rs` (`PuppetMap::ensure_puppet`).
- **Key constraint (R3):** the `/brehon/room-event` handler MUST dispatch via `tokio::spawn` and return 200 immediately — never block the ACK on a Matrix call.
- **Idempotency key:** `(case_id, room_type)` — checked via `bridge_room::lookup` before any createRoom call.
- **C2.1 jury path only** (T3 adds C2.2–C2.6).
- **R8:** bridge uses `anyhow::Result`, axum — no Lemmy-workspace lessons, no `--features full`, no `LemmyError`.
- **validate-pending-laptop:** `commands: ["cd services/bridge && cargo check"]`, `phase_task: 2`.
- **rusqlite pin is `0.37`** (already in Cargo.toml on phase branch — do NOT re-specify or change).

---

## ASSUMES / VERIFY before acting (per feedback_handover_assumptions_need_empirical_verification)

| Assumption | How to verify (≤5 min each) |
|---|---|
| Daemon still on `phase-m2-rooms-a` | `ssh homeserver 'cd /srv/brehon-fork && git branch --show-current'` |
| DQ pending[] is empty | `ssh homeserver 'python3 -c "import io,json; d=json.load(io.open(\"/srv/brehon-fork/.claude/decision-queue.json\",encoding=\"utf-8\")); print(len(d[\"pending\"]))"'` |
| No Junior tasks running | `mcp__junior-brehon__list_tasks(status="running")` |
| phase-m2-rooms-a tip is `56de6da77` | `git ls-remote origin phase-m2-rooms-a` |
