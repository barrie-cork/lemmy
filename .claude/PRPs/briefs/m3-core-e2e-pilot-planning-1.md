# m3-core-e2e-pilot — planning brief

`[role:planning]` Author the M3 Phase-6 (FINAL) plan: **full town-hall acceptance e2e + a real pilot town-hall run end-to-end (D2)**. This is the *integration + pilot* phase, NOT a code-add phase. Phases 1–5 (infra, entry-kinds, stage-mode, emergency-mute, recording) shipped the RTC control logic + `#[ignore]`-gated `todo!()` test scaffolds; Phase 6 (a) brings up the live RTC sidecar stack the acceptance tests need (MinIO + a two-instance federation topology — both ABSENT today), (b) turns the existing `#[ignore]`/`todo!()` scaffolds into live integration tests that pass the §Success Criteria, (c) wires the two recording carry-forwards (cr-2 live session-auth, cr-3 participant-set; recording live trigger = real Egress POST + S3 PUT), and (d) scopes the **D2 pilot** as a human-run operational acceptance checklist (NOT an impl-task). The marquee risk is the **federation-wide emergency-mute <500ms measured at the publisher client across two bridge instances** (PRD line 141).

## 1. Role + dispatch

`[role:planning]` — produce `.claude/PRPs/plans/m3-core-e2e-pilot.plan.md` from the M3 sub-PRD Phase 6, following `.claude/commands/prp-plan.md` + `.claude/PRPs/templates/plan.template.md`. Plan-shaping only — author NO implementation code, NO `crates/**`, NO `services/bridge/**`. The plan is the deliverable.

**This phase has TWO halves with different shapes — the plan MUST scope both and keep them distinct:**
- **Half A (impl/cohort flow):** the harness bring-up + the acceptance-e2e tests + the two recording carry-forwards. Normal §13 tasks, `validate-pending-laptop[-e2e][-linux]` gates, bridge-Linux-only.
- **Half B (operational, NON-impl):** the D2 pilot town-hall run. This is a human-run acceptance checklist the advisor coordinates + records in the retro — NOT a Junior impl-task. The plan scopes it as a §13 *checklist/runbook task* explicitly marked "operator-run, not impl" with no cargo DoD. **A §13 task that tries to "implement the pilot" is a SCOPE ERROR — raise a `kind: "blocker"` DQ** (bootstrap tripwire / watchpoint #6).

## 2. Scope

Plan **all of M3-core-e2e-pilot Phase 6 in one plan**. Source: `.claude/PRPs/prds/m3-town-halls-rtc.prd.md` §"Phase 6: M3-core e2e + pilot" (lines 250–253) + §Success Criteria (the FULL table, lines 134–149 — Phase 6 is the phase that proves the whole table) + §Technical Approach risk rows (196–203, esp. the federation-mute risk row 198 + the RTC-topology row 202) + D2 (line 269) + D6 (line 273).

The plan's §13 tasks cover (planner finalizes count + `[P]` markers + ordering; the harness-bring-up task MUST be Task 0/1 and a hard `requires:` dependency of every acceptance test):

1. **RTC e2e harness bring-up — MinIO + two-instance federation topology (the load-bearing net-new infra; prove-the-harness-reaches-the-containers BEFORE acceptance).** The existing `services/bridge/docker-compose.yml` ships the single-instance RTC stack (`tuwunel` Matrix HS + `livekit` + `lk-jwt-service` + `element-call`) but is **MISSING the two things Phase 6 acceptance needs**: (a) **MinIO** (the recording store — required by the `recording_lands_with_hash_on_chain` + `participant_floor_fetch` tests, D4/D5); (b) **a second federated bridge+homeserver instance** (required by the marquee `mute_all_drops_all_publishers_cross_instance_under_500ms` test — cross-instance is meaningless on one instance). This task extends the compose (or adds a Phase-6 e2e override compose, e.g. `services/bridge/docker-compose.e2e.yml`) with MinIO + the second instance, and proves the harness can **reach** the containers — a smoke test that brings the stack up and confirms bridge↔livekit↔minio↔second-instance connectivity — **before** any acceptance test depends on it. Per bootstrap §3 + watchpoint stop-and-ask: "if the e2e suite cannot reach the LiveKit/MinIO testcontainers, prove the harness can reach them in an ISOLATED task BEFORE queueing the acceptance tests, or the acceptance failures will be unattributable." This is the same risk-isolation pattern the recording plan §18 used for the S3 dep. **Watch the two-Lemmy-on-one-host federation collision** (`feedback_lemmy_federation_domain_collision_one_host.md`, BUG-15): Lemmy keys by PORT-STRIPPED domain, so the second instance needs a distinct hostname/domain, not just a distinct port. See §4.

2. **Recording carry-forwards: cr-2 live session-auth + cr-3 participant-set (turn the recording scaffold live).** The recording phase shipped `LiveSink::trigger_egress`/`upload` as fail-closed `bail!` stubs and a participant-floor that returns 403 for ALL fetches (correct-by-default; nothing wired). Phase 6 wires: (cr-2) requester-pseudonym binding from a real session auth so the fetch handler knows WHO is asking; (cr-3) the real participant-set lookup so the floor can ADMIT a participant (today every caller is rejected). Plus the recording live trigger: real Egress POST (via the LiveKit JWT path that infra built) + real S3 PUT. After this task the three `services/bridge/tests/recording.rs` scenarios (`recording_lands_with_hash_on_chain`, `clean_posture_no_recording_when_disabled`, `participant_floor_fetch`) become live `--ignored` integration tests. Source: `m3-core-recording-retro.md` §6 carry-forwards 1+2 + the cr-2/cr-3 triage rows. **Constraint preserved from recording:** `content_sha256` still rides `append_room_event` (never a bypass digest), pseudonyms-only in the chain entry. See §4.

3. **Make the acceptance e2e tests live (turn `#[ignore]`/`todo!()` scaffolds into passing integration tests).** The scaffolds exist with step-by-step comment bodies; this is "implement against the live docker-compose stack (Phase-6 pilot grade)" — the `todo!()` markers in each file. The live-test targets (verified 2026-06-20, branch `governance-v0` @ `2fae33bc1`), grouped by §Success-Criteria coverage:
   - **`services/bridge/tests/stage_mode.rs`** → `four_mic_pass_then_grace_boundary_emits_chair_entries` (covers criteria: 4-user mic-pass in sequence [line 137], 30s grace auto-revoke [138], chair override `room_chair_override` [139], chair transfer `room_chair_transferred` [140]).
   - **`services/bridge/tests/emergency_mute.rs`** → `mute_all_drops_all_publishers_cross_instance_under_500ms` (the MARQUEE — criterion line 141). **Measured at the PUBLISHER CLIENT across two federated bridge instances, NOT the server.** The scaffold body already pre-specifies this (per-publisher publish-active baseline at the publisher client; mute via Matrix power-levels federated to the remote instance + the in-instance LiveKit belt-and-suspenders; assert every non-chair publisher's publish right revoked <500ms at the publisher client; zero-holder cross-instance negative invariant; chain entry `{chair_pseudonym, federated: true}`). It also pre-documents the PRD fallback (best-effort SLA + in-instance <500ms guarantee if cross-instance Matrix propagation proves infeasible at pilot — DOCUMENT, do not silently drop). **A test that measures mute latency server-side is a FALSE GREEN — catch-fire (watchpoint #2 / bootstrap §7).**
   - **`services/bridge/tests/recording.rs`** → the three scenarios from Task 2 (recording-lands [143], clean-posture [145], participant-floor fetch [144]).
   - **`services/bridge/tests/room_provisioning.rs`** (already has 4 live-able scenarios incl. `messaging_disabled_prevents_provisioning`) → confirm the `rtc_enabled=false` clean-posture criterion (line 146 — "zero RTC provisioning, entire stack absent; governance flow unchanged") is covered here OR add a town-hall-specific clean-posture assertion. The anonymous-town-hall pseudonym-overlay criterion (line 142 — "real identity never reaches LiveKit") needs a home: either the infra-phase JWT-issue-time identity test already covers it (cite it) or a new assertion lands here.
   The planner maps EVERY §Success-Criteria row (134–149) to exactly one live test fn (bare fn name) in §16a; an uncovered criterion is a planning gap to surface as a DQ.

4. **D2 pilot run — operator checklist (NON-impl, Half B).** Scope a §13 task that is a **human-run operational acceptance runbook**, explicitly "operator-run, not impl; no cargo DoD." Source: D2 (line 269) + Success Criteria line 149 ("Pilot town hall runs end-to-end — the real acceptance signal"). The runbook: bring up the full RTC stack on the pilot server (`http://100.81.145.58:1236`, accounts in `reference_pilot_test_accounts.md`), open a concrete pilot town hall (community-deliberation broadcast OR appeal hearing with audience), the chair passes the mic to ≥1 user, optionally enable recording and confirm it lands with its hash entry. The DoD is a recorded outcome in the retro (did a real town hall run? did the chair pass the mic? did recording land?), NOT a test pass. The advisor coordinates + records this; the planner scopes the checklist + the go/no-go criteria. **This task has no `validate-pending-laptop` gate** — it is not impl.

**Explicit boundaries — do NOT plan:**
- **No new entry-kind const, no `ROOM_KINDS` add, no registry-count bump.** Registry count is FROZEN at 72 (`crates/db_schema/src/source/governance/governance_log.rs`; `.claude/rules/governance-log-entry-kind-registry.md`). The 3 chair/mute kinds (`room_chair_transferred`, `room_chair_override`, `room_mute_all`) shipped in stage-mode/emergency-mute; `room_recording_uploaded` shipped in M2 + emitted in recording. Phase 6 EXERCISES existing kinds; it adds NONE. A plan task proposing a new `ENTRY_KIND_ROOM_*` const is a SCOPE ERROR — stop and raise a `kind: "blocker"` DQ (bootstrap tripwire / watchpoint #4).
- **No new migration.** This is acceptance + pilot. A `crates/db_schema/migrations/**` or `services/bridge` embedded-schema column addition is a scope error — surface as a `kind: "blocker"` DQ.
- **No new Brehon-workspace Rust features.** The control logic is scaffolded. The only allowed `crates/**` changes are wiring the recording carry-forwards (cr-2/cr-3) IF they need a binary-side change (e.g. the participant-set query the bridge calls); favour bridge-side. The zero-RTC-deps-in-`--workspace` invariant holds.
- **No strict presigned-URL ACL / retention / tombstone / GDPR-erasure** — DEFERRED (D5 Option C). Participant-floor only.
- **No cross-instance recording-store federation** — recordings stay instance-local (OQ-V2-07). The cross-instance topology in Task 1 is for the MUTE marquee, NOT for replicating recordings.
- **No "implement the pilot" as a cargo impl-task** — D2 is operational (Half B). Catch-fire if a task tries (watchpoint #6).
- **No measuring emergency-mute latency server-side** — publisher-client only (watchpoint #2). Catch-fire.

## 3. Required reading

- `.claude/PRPs/prds/m3-town-halls-rtc.prd.md` — §"Phase 6" (250–253), the FULL §Success Criteria table (134–149 — Phase 6 proves all of it), §Technical Approach risk rows (196–203, esp. row 198 [federation mute >500ms — the H3 headline] + row 202 [RTC topology / OQ-V2-10 non-loopback bridge↔LiveKit]), §Decisions D2 (269), D6 (273).
- `.claude/PRPs/handovers/m3-core-e2e-pilot-bootstrap.md` — the FULL handoff letter. §1 (two-halves framing), §2 (why this is the HARD M3-core phase), §3 (lessons that apply), §4 watchlist (the SIX phase-specific items), §6 (gate-4 NOW LIVE + the new runner kinds), §7 catch-fire, §"Stop-and-ask tripwires", §"Git state at handoff."
- `.claude/PRPs/reports/m3-core-recording-retro.md` — §6 Carry-forwards (1: cr-2/cr-3 live session-auth; 2: recording live trigger Egress POST + S3 PUT) + the cr-2/cr-3 triage rows (lines 26–27) + §8 (Copilot gap / MiniMax-as-reviewer — now LIVE, see below). These two carry-forwards ARE Task 2.
- `.claude/PRPs/plans/m3-core-recording.plan.md` + `.claude/PRPs/plans/m3-core-emergency-mute.plan.md` — the SIBLING plans (canonical-schema-first gate, `feedback_read_canonical_before_writing_spec.md`). Mirror their §5 complexity breakdown, §7 preflight guardrails, §10 patterns-to-mirror, §15 three-validation-surface DoD shape, §16a story shape (negative-invariant marquee + ADR-015 pin + observable-state assertions). Emergency-mute's `mute_all` test is the immediate ancestor of THIS phase's marquee — match its publisher-client measurement framing exactly.
- `.claude/PRPs/plans/m3-core-stage-mode.plan.md` — the plan that built the bridge→binary callback client + the `EmitIntent`/`pending_emits`/`drain_emits` seam + the chair state machine the `stage_mode.rs` test exercises. Confirm the seam Phase 6 EXERCISES (do not rebuild).
- `docs/brehon-law-inspired-network/04-data-model-and-api.md` — canonical schema; the `RoomEventPayload` shape + `ROOM_KINDS` array + the `POST /api/v4/governance/room-event` route. CODE WINS where 04 predates the RTC plane (doc-drift row 203).
- ADR-015 (pseudonymity / `always_pseudonym` — recording + chair entries carry pseudonyms, real identity never reaches LiveKit), ADR-016 (backplane — content never hashed beyond `content_sha256`; `federated: true` metadata), ADR-013 (EmergencyRemove is DISTINCT from the chair "emergency mute" — line 157; do not conflate), ADR-008 (append-only signed log), ADR-011 (AGPL — MinIO/Element-Call AGPL-3.0, LiveKit/lk-jwt Apache-2.0; licence-clean per bootstrap §2) in `docs/brehon-law-inspired-network/99-decisions-and-open-questions.md`.
- `reference_pilot_test_accounts.md` (PMD System-1) — pilot server `http://100.81.145.58:1236`, juror1-5 + testmod/testuser creds, `test_governance` community, API endpoints (for the D2 runbook in Task 4).
- `workflow_state_pilot_internal.md` (PMD System-1) — the pilot-internal track state (phases 1+2 DONE 2026-06-13, gov→Matrix verified; home-net reachability + go/no-go pending). The D2 pilot town hall coordinates with this track — read for what's already proven (gov→Matrix path) vs what Phase 6 adds (the live RTC floor).
- **Reconnaissance file:line anchors (verified 2026-06-20, branch `governance-v0` @ `2fae33bc1`):**
  - **Live-test scaffold targets** (all `#[tokio::test] #[ignore = "requires docker-compose stack"]`, body `todo!("implement against live docker-compose stack (Phase-6 pilot grade)")`):
    - `services/bridge/tests/stage_mode.rs:14` `four_mic_pass_then_grace_boundary_emits_chair_entries()`
    - `services/bridge/tests/emergency_mute.rs:14` `mute_all_drops_all_publishers_cross_instance_under_500ms()` (MARQUEE; scaffold body lines ~15–45 pre-specify publisher-client measurement + the PRD fallback)
    - `services/bridge/tests/recording.rs:14` `recording_lands_with_hash_on_chain()`, `:33` `clean_posture_no_recording_when_disabled()`, `:47` `participant_floor_fetch()`
    - `services/bridge/tests/room_provisioning.rs:14,25,46,57` (4 scenarios; `messaging_disabled_prevents_provisioning` at `:57` is the clean-posture model)
  - **RTC harness compose (EXTEND in Task 1 — MinIO + 2nd instance ABSENT today):** `services/bridge/docker-compose.yml` — has `tuwunel:` (`:19`, Matrix HS), `livekit:` (`:53`, `livekit/livekit-server:v1.8` Apache-2.0, `--dev` standalone devkey/devsecret), `lk-jwt-service:` (`:59`, `ghcr.io/element-hq/lk-jwt-service:0.3.0`), `element-call:` (`:68`, `ghcr.io/element-hq/element-call:v0.6.0` AGPL-3.0). **NO `minio:` service. NO second federated instance.** Network `bridge-net:` at `:92`.
  - **Recording carry-forward seams (turn live in Task 2):** `services/bridge/src/recording.rs` — `LiveSink::trigger_egress`/`upload` (fail-closed `bail!` stubs per recording retro §6.2); `compute_content_sha256`; `RecordingSink` trait. The participant-floor fetch handler (returns 403 for all callers until cr-3 wires the participant set).
  - **Emit seam (EXERCISED, not rebuilt):** `services/bridge/src/stage.rs` — `EmitIntent` + `pending_emits` + the chair-action pushes; `room_event_client::drain_emits` → `post_room_event` → binary `crates/api/api/src/governance/governance_log.rs:238 append_room_event` (gates on `ROOM_KINDS`, runs `scrub_json` + ed25519 hash-chain + signing).
  - **`cargo-linux.sh`:** `scripts/brehon/cargo-linux.sh` (present) — the ONLY way bridge cargo/test runs (Docker `rust:1.95`).
- **Lessons (mandatory for the planner to honour in §4/§15/§16a):**
  - `feedback_bridge_validates_on_linux_not_windows.md` — bridge cargo/test runs on Linux ONLY (`scripts/brehon/cargo-linux.sh ... --manifest-path services/bridge/Cargo.toml`; `ruma-common` E0119 on Windows). EVERY bridge DoD command uses this form. A §15 bridge command in the Windows `cd services/bridge && cargo` form is a DoD-issue → file advisor DQ (watchpoint #3).
  - `feedback_e2e_nextest_filter_groups.md` — scope e2e runs with `-E` / `e2e_filter`; the town-hall group, not the full ~12-min suite, where possible. Now LOAD-BEARING (e2e is the phase's centre of gravity). `e2e_filter` field goes in the `validate-pending-laptop-e2e` DQ.
  - `feedback_lemmy_error_no_std_error.md` (Case A/B) — the bridge tests return `anyhow::Result<()>` (not `Box<dyn Error>`), so this is lower-risk here, but any NEW `crates/server/tests/e2e.rs` test returning `Result<(), Box<dyn Error>>` triggers it. (The town-hall tests live BRIDGE-side `anyhow`, NOT `crates/server/tests/e2e.rs` — 0 town-hall matches there; correct the bootstrap's implication that they're in `crates/server/tests/e2e.rs`.)
  - `feedback_fix_impl_pre_locate_e2e_anchors.md` — for any e2e-edit task ≥2 edits, pre-locate verbatim `old_string`/`new_string` anchors; the uniqueness gate (`grep -c '<anchor>'` == 1) is mandatory before queuing. The `todo!()` bodies are the natural unique anchors here (one per fn).
  - `feedback_lemmy_federation_domain_collision_one_host.md` (BUG-15) — the two-instance federation topology (Task 1) must give the second instance a DISTINCT hostname/domain (Lemmy/Matrix key by port-stripped domain). Same host:port → identity confusion → the cross-instance mute test can't distinguish instances.
  - `feedback_authz_state_machine_test_asserts_negative.md` — the `clean_posture_no_recording_when_disabled` + `rtc_enabled=false` tests are NEGATIVE invariants: assert NO side-effect. Mechanical check: **does the test fail if you delete the flag-gate (force the path on)?** If it stays green, it asserts the happy path, not the invariant.
  - `feedback_build_what_tests_exercise.md` + `pattern_test_against_reality_not_syntax.md` — the live tests must EXERCISE the real flow (real Egress + MinIO object + the real chain entry; real cross-instance publisher-side mute timing) and assert on OBSERVABLE state (MinIO object exists, chain row present, publish-right revoked at the publisher client, HTTP status), not config/struct shape.
  - `feedback_cheap_model_arm_drops_adr_constraints.md` — the ADR-015 pseudonym pins (cr-2 requester-pseudonym, recording chain-entry identity fields) must be LOAD-BEARING, not named (§2.4a).
  - `feedback_complexity_score_pre_split.md` — this phase's e2e edits are ≥2 → expect the `feedback_fix_impl_pre_locate_e2e_anchors.md` injection; plan §5 complexity score will likely be >8 (sidecar bring-up + cross-instance + 8 live scenarios) → the planner must resolve a split-or-proceed decision.

## 4. Constraints (enforce in the plan)

- **Emergency-mute <500ms is measured at the PUBLISHER CLIENT across two federated bridge instances, NEVER the server (CATCH-FIRE-CLASS, LOAD-BEARING — the marquee).** Per Success Criteria line 141 + PRD risk row 198 + bootstrap watchpoint #2 + tripwire #1. The §16a story for `mute_all_drops_all_publishers_cross_instance_under_500ms` MUST: (a) record a per-publisher publish-active baseline at the publisher client; (b) fire mute-all; (c) assert EVERY non-chair publisher's publish right is revoked, measured at the publisher client, within 500ms; (d) assert the cross-instance zero-holder negative invariant (no listed publisher on EITHER instance retains publish); (e) assert the chain entry `{chair_pseudonym, federated: true}`. A server-side measurement is a FALSE GREEN — catch-fire. The plan must also encode the PRD fallback (best-effort SLA + in-instance <500ms guarantee, DOCUMENTED via a DQ before declaring the criterion failed — do NOT silently drop the cross-instance requirement). Name the publisher-client measurement point (which client API / which timestamp) in §4 of the plan, not "measure at the publisher."

- **`content_sha256` rides `append_room_event`, NEVER a raw digest (CATCH-FIRE-CLASS, inherited from recording).** The recording's `content_sha256` reaches the chain via `append_room_event` (`crates/api/api/src/governance/governance_log.rs:238`, runs `scrub_json` + ed25519 hash-chain + signing). Computing the SHA256 with `sha2` is fine; the CONSTRAINT is it reaches the chain via `append_room_event`, not a side channel. The live `recording_lands_with_hash_on_chain` test asserts the `content_sha256` appears in the `governance_log` chain row with a valid signature/prev-hash link, not merely in a MinIO object's metadata.

- **`record_town_halls=false` AND `rtc_enabled=false` produce ZERO side-effects (HARD DoD INVARIANTS, LOAD-BEARING).** Two clean-posture criteria (lines 145 + 146). Each §16a clean-posture story asserts the NEGATIVE (no Egress / no MinIO write / no chain entry for recording-off; no LiveKit/MinIO provisioning at all for `rtc_enabled=false`) and MUST fail if the flag-gate is removed (`feedback_authz_state_machine_test_asserts_negative.md`). A clean-posture story that is a trivial always-skip is invalid — surface it as a §3.5 watchpoint failure.

- **ADR-015 pseudonym pins — LOAD-BEARING, not named** (§2.4a + `feedback_cheap_model_arm_drops_adr_constraints.md`):
  1. **cr-2 requester-pseudonym binding:** the fetch handler resolves WHO is asking from a real session auth as a PSEUDONYM; the participant-floor check (cr-3) compares pseudonym against the room participant set. Cite the fn + callsite the planner picks; DoD grep: `grep <participant-floor-fn> <fetch-handler>` returns BOTH definition AND a callsite in the fetch path.
  2. **Chain-entry identity fields are pseudonyms:** every `room_*` payload (chair entries, recording `speakers`/actor) carries pseudonyms — NEVER `person_id`/username/MXID. A real identity in a hash-chained append-only entry is permanent and unrecoverable; a leak breaks `always_pseudonym` for the whole M3 cluster.
  3. **Anonymous-town-hall real identity never reaches LiveKit** (criterion line 142): the §16a story asserts the LiveKit server receives only the pseudonym (mirror the infra-phase JWT-issue-time identity test, OR cite that infra already covers it and the floor inherits it — name where it's tested, don't silently assume).

- **Two-instance federation topology needs distinct domains** (`feedback_lemmy_federation_domain_collision_one_host.md`, BUG-15). The second instance (Task 1, for the cross-instance mute marquee) MUST have a distinct hostname/domain, not just a distinct port — Lemmy/Matrix key by port-stripped domain; same host:port → identity confusion → the cross-instance test can't tell the instances apart. The plan's §4 names the two domains and how the federation link is established (the `docker-compose-fed-enable.yml` override pattern is the existing fed-test precedent).

- **Bridge compiles + tests on Linux only.** Every `services/bridge/**` task's DoD = a `validate-pending-laptop-linux` (compile) and/or `validate-pending-laptop-e2e` (the `--ignored` integration run) DQ; the cargo commands are `scripts/brehon/cargo-linux.sh check --manifest-path services/bridge/Cargo.toml` and `scripts/brehon/cargo-linux.sh test --manifest-path services/bridge/Cargo.toml --test <name> -- --ignored`. The Windows `cd services/bridge && cargo` form is a DoD-issue (`ruma-common` E0119). The §15 commands MUST be written in the EXACT form the advisor runs at gate 1 (`feedback_plan_dod_dry_run_at_write.md`) — dry-run them mentally before committing the plan. **Note: the `--ignored` integration tests need the full docker-compose RTC stack UP (Task 1's harness) — they are NOT pure `cargo-linux.sh` compile-proofs; the planner distinguishes the compile gate (`-linux`) from the live integration gate (`-e2e`, needs the stack) in §15.**

- **gate-4 (Phase-2 e2e local-vs-dispatch) is LIVE this phase — the plan must NOT pre-pick it.** This is the first M3-core phase where the e2e suite needs Docker + LiveKit/MinIO + two-instance testcontainers (heavier than prior phases). The advisor surfaces the local-vs-dispatch choice ONCE per phase to the user (never auto-pick after PR #105). The plan should NOTE which §13 tasks carry the live `-e2e` gate (the ones that turn scaffolds live), so the advisor knows when gate-4 fires — but the plan does not choose local vs dispatch.

- **Bare fn name for bridge test filters** (promoted from emergency-mute L1). Every §15/§16a test-filter uses the BARE fn name (e.g. `mute_all_drops_all_publishers_cross_instance_under_500ms`), NOT `module::tests::...`. Add to §7/§15 as an explicit constraint.

- **No registry-count change, no migration, no new entry-kind const** (boundaries §2). The plan that touches any of these is a scope error — `kind: "blocker"` DQ.

- **The D2 pilot is operational, NOT impl** (Half B). The pilot §13 task has NO cargo DoD, NO `validate-pending-laptop` gate; its DoD is a recorded retro outcome. A task that implements the pilot as cargo work is catch-fire (watchpoint #6). The plan scopes the runbook + go/no-go criteria; the advisor coordinates the run.

- **Watchpoint specificity** (§3.5 gate, `feedback_advisor_watchpoint_specificity.md`): every §4 plan watchpoint cites a specific file:line / fn / test — e.g. "watch `mute_all` asserts publish-right revoked at the publisher client (cite the client API + timestamp) not the server", "watch the recording test asserts the MinIO object exists AND the `room_recording_uploaded` chain row carries `content_sha256` via `append_room_event`", "watch the second instance has a distinct domain (cite the compose service names)" — not "watch the mute test" or "watch federation."

- **§16a stories** must map EVERY §Success-Criteria row (134–149) to exactly one live test fn (bare name), each asserting on observable state. At minimum: the cross-instance <500ms mute MARQUEE (publisher-client measured, the delete-server-side-measurement check); the recording-lands marquee (`content_sha256` on chain); BOTH clean-posture negative invariants (recording-off + `rtc_enabled=false`, each satisfying the delete-the-gate check); the participant-floor fetch (non-participant rejected, participant succeeds, ADR-015 pseudonym pin); the 4-user mic-pass + 30s grace + chair override + chair transfer; the anonymous-town-hall identity-never-reaches-LiveKit. PLUS the Half-B pilot acceptance story (operator-run; DoD = retro-recorded outcome, not a cargo gate). An uncovered §Success-Criteria row is a planning gap — DQ it.

- **DQ discipline:** planner writes `from: "planner"`; pre-seeds OQs as `answered_by: "planner"` only; mid-task pushes per `decision-queue.md` "Mid-task visibility." Likely DQs: (1) the publisher-client measurement mechanism for the <500ms mute (which LiveKit client API / how the publisher-side timestamp is captured in a test) — raise as `kind: "blocker"` with options if non-obvious; (2) the second-instance topology (extend `docker-compose.yml` vs a new `docker-compose.e2e.yml` override) — pre-seed a recommendation; (3) the gate-4 local-vs-dispatch note (NOT a planner decision — flag for the advisor's user gate).

---

**Brief authored on `governance-v0`, committed before `create_task` (planning briefs commit on trunk — `.claude/refs/auto-phase.md` §"Brief location per role").** Pre-planning OQ-resolvability gate cleared: all M3 OQs (OQ-V2-04/05/06/07/08/10 + OQ-009) are RESOLVED (PRD §Blocking Dependencies line 259); no parked OQ blocks Phase 6. Lessons fired (recon + bootstrap-watchlist driven; PMD `memory_search_hybrid` ran — returned bridge-impl retros confirming the `validate-pending-laptop` brief-friction pattern [PMD ID 864: never pair a per-commit cargo DoD with the laptop-validation stop], internalised for §15; §2.4 mechanical injection is impl/fix-impl-only): bridge-validates-on-linux, e2e-nextest-filter-groups, lemmy-federation-domain-collision, authz-asserts-negative, build-what-tests-exercise, test-against-reality-not-syntax, ADR-015-load-bearing, fix-impl-pre-locate-e2e-anchors, complexity-score-pre-split.

**Key recon findings surfaced for the planner:**
1. **The acceptance e2e harness is genuinely net-new infra** — `services/bridge/docker-compose.yml` has the single-instance RTC stack (tuwunel + livekit + lk-jwt + element-call) but is MISSING **MinIO** (recording store) AND a **second federated instance** (cross-instance mute). Task 1 (harness bring-up + reach-the-containers smoke) is load-bearing and a hard `requires:` of every acceptance test.
2. **The scaffolds are well-formed** — each live-test target is `#[tokio::test] #[ignore = "requires docker-compose stack"]` with a step-by-step comment body and a single `todo!("...Phase-6 pilot grade")` — the `todo!()` markers are the natural unique Edit anchors. The marquee `emergency_mute.rs` test even pre-specifies publisher-client measurement + the PRD fallback. This is scaffold-to-live, not greenfield.
3. **The town-hall e2e is BRIDGE-side `anyhow::Result`, NOT `crates/server/tests/e2e.rs`** (0 town-hall matches there) — correct the bootstrap §4 watchpoint #1 implication. The `feedback_lemmy_error_no_std_error.md` Case-A override is lower-risk here (anyhow, not `Box<dyn Error>`), but still injected for any new `crates/server/tests/e2e.rs` work.
4. **Two carry-forwards from recording ARE Task 2** — cr-2 (live session-auth requester-pseudonym) + cr-3 (real participant-set so the floor can ADMIT). The recording floor returns 403 for ALL callers today (correct-by-default); Phase 6 makes it admit participants.
5. **MiniMax governance-ai-review is now LIVE** (shipped 2026-06-20) — the post-Copilot second reviewer on small (<28KB) governance PRs into `governance-v0`. Its first real API exercise is likely a Phase-6 fix-in-PR; `bm-poll-cr` may need a `source: minimax` bucket (carry-forward from recording retro §8).
6. **MARQUEE is distributed-systems, not storage** — the federation-wide <500ms mute (Matrix power-level propagation across two instances, measured at the publisher client) is the H3 headline risk. The plan front-loads it (Task 1 builds the topology) and encodes the publisher-client measurement + the documented PRD fallback.
