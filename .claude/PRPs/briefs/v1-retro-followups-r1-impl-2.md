# v1-retro-followups-r1 — impl Task 2 brief (Cohort 1 [P], impl-task contract)

## 1. Role + dispatch line

`[role:impl-task] v1-retro-followups-r1 Task 2 — live-state validation § in impl-task contract — see .claude/PRPs/briefs/v1-retro-followups-r1-impl-2.md`

## 2. Scope

Implement plan §13 **Task 2** of `.claude/PRPs/plans/v1-retro-followups-r1.plan.md`. One commit, one file modified:

```yaml
creates: []
modifies:
  - .claude/agents/impl-task.md
requires:
  - task: 0
```

**Branch context:**
- `base_branch` = `governance-v0` at trunk tip.
- Worker forks `governance-v0` → worktree branch `junior/<auto-generated>`.
- One commit; daemon finalize-merges (or advisor manual-finalize on DQ #338 recurrence).

**Do NOT** author or modify:
- `.claude/agents/planning.md`, `bm-task.md`, `ci-watcher.md` (this task only touches impl-task.md — the role whose validation discipline triggered the finding).
- `.claude/lessons/feedback_multi_lane_worktree_discipline.md` (Task 1).
- `.claude/rules/advisor-orchestrator.md` (Task 3).
- Any other `.claude/agents/*.md` file.
- Any file under `crates/`, `migrations/`, `tests/`, `docs/brehon-law-inspired-network/**`, `scripts/brehon/`.

**`[P]` cohort discipline:** dispatched in parallel with Task 1 + Task 3. FILES YAML disjoint by construction.

## 3. Required reading

### 3.0 Plan + governing brief (P0)

1. `.claude/PRPs/plans/v1-retro-followups-r1.plan.md` §1, §3, §10.2 (agent-contract-extension shape MIRROR), §13 Task 2 (full task body), §15.1 (DoD), §16a Story 2.
2. `.claude/PRPs/reports/session-retro-2026-05-22-v1-dq-schema-r1-cohort-2-ship.md` §"What surprised us" #1 (the TypeError discovery) + §"What to change" #3.

### 3.1 MIRROR refs (P0 — read BEFORE first Edit)

3. `.claude/agents/impl-task.md` (full file) — current shape. Identify a suitable insertion point for a new H2 § per Constraint #5 below.
4. `.claude/agents/planning.md` (look for `## DQ schema-v3 (post-v1-dq-schema-r1)`) — added by v1-dq-schema-r1 Task 2 (commit `1803b8546`). Mirror the H2 § shape (one-paragraph discipline note + practical "How to apply" sub-block + cross-reference).
5. `.claude/lessons/pattern_test_against_reality_not_syntax.md` — the canonical pattern Task 2's § reinforces. Read so the cross-reference is precise.

### 3.2 Lessons that bind §13 decisions

6. `.claude/lessons/feedback_test_scripts_against_real_targets.md` — sibling pattern; the new § builds on it.
7. `.claude/lessons/feedback_verify_files_with_read.md` — related discipline (verify state before assuming).
8. `.claude/lessons/feedback_principles_not_rules.md` — prose tone (plain language, no keyword stuffing).
9. `.claude/lessons/feedback_junior_pmd_write_convention.md` — LESSON-trailer discipline.

### 3.3 Rules

10. `.claude/rules/phase-branch.md` — direct-commit policy.
11. `.claude/rules/decision-queue.md` "Mid-task visibility" — push immediately after a blocker DQ raise.

## 4. Constraints

1. **New § header verbatim:** `## Live-state validation (post-v1-retro-followups-r1, 2026-05-22)` — including the parenthetical date. §15.1 grep depends on this exact match.
2. **Required content keywords (§15.1 grep targets):** body MUST contain the literal strings `pattern_test_against_reality_not_syntax` and `TypeError` (each ≥1 occurrence). The TypeError reference is the recurrence evidence; the pattern cross-reference is the canonical link.
3. **v1-dq-schema-r1 Task 3 citation:** the recurrence evidence is the hidden `TypeError` in `resolve-dq-canonical.sh`'s sort key — body MUST cite this specifically ("v1-dq-schema-r1 Task 3 surfaced a hidden `TypeError`…"). This grounds the abstract discipline in a concrete incident.
4. **Position: insertion-point selection** — `.claude/agents/impl-task.md` has multiple H2 §s. The new § goes ALPHABETICALLY adjacent to existing `## Live-*` sections IF ANY (e.g. after `## Live-state validation` would go after any `## Live-` precedent and before `## Loop discipline`). If no `## Live-*` §s exist, insert at end of file BEFORE any `## See also` / cross-reference § at file tail. Do NOT relocate existing §s.
5. **EXTENDS the role's task-0 checklist conceptually, NOT a literal task-0 modification.** The new § is documentary discipline — a future impl-task subagent reads the new § during its mandatory §3 Required reading consumption. The advisor briefs cite it. Do NOT add a literal step to any existing checklist § in this same edit (that would be a second cross-cutting change).
6. **No new file created.** The change IS the new H2 §.
7. **Worker branch + mid-task push** per `decision-queue.md` "Mid-task visibility".
8. **Attribution integrity:** `from: "impl"` only. NEVER `answered_by: "advisor"` / `"user"`. Self-resolve with `"impl-self-resolved"` ONLY when answer is in codebase / plan / lessons.
9. **No cargo.**
10. **Commit subject template:** `feat(retro-followups-r1): live-state validation § in impl-task contract (task 2)`.
11. **Single commit.**
12. **No `--no-verify`.**

### Branch + commit discipline

Same as Task 1: worker branch `junior/role-impl-task-<auto-id>`, single commit, push immediately, daemon finalize-merges back.

### Lesson trailer (encouraged)

Append `LESSON: <one-line>` trailer if a durable learning surfaces.

## 5. Validation gates (per plan §15.1 Task 2 + §16a Story 2)

```bash
grep -c '^## Live-state validation (post-v1-retro-followups-r1' .claude/agents/impl-task.md
# EXPECT: 1

grep -c 'pattern_test_against_reality_not_syntax' .claude/agents/impl-task.md
# EXPECT: ≥1

grep -c 'TypeError' .claude/agents/impl-task.md
# EXPECT: ≥1
```

All three checks MUST pass before commit + push.

## 6. Expected output (return to advisor)

```
## Task 2 complete — v1-retro-followups-r1 live-state validation §

**Branch:** junior/<task-id>
**Commit:** <sha> — feat(retro-followups-r1): live-state validation § in impl-task contract (task 2)
**Files modified:** 1 (.claude/agents/impl-task.md)
**§15.1 grep checks:** 3/3 pass
**Pushed:** yes
**Validate-pending DQ:** N/A (no cargo)
**LESSON trailer:** <yes/no>
```
