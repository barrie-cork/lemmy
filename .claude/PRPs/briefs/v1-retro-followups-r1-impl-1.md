# v1-retro-followups-r1 — impl Task 1 brief (Cohort 1 [P], multi-lane lesson)

## 1. Role + dispatch line

`[role:impl-task] v1-retro-followups-r1 Task 1 — pre-commit hygiene § in multi-lane lesson — see .claude/PRPs/briefs/v1-retro-followups-r1-impl-1.md`

## 2. Scope

Implement plan §13 **Task 1** of `.claude/PRPs/plans/v1-retro-followups-r1.plan.md`. One commit, one file modified, zero files created:

```yaml
creates: []
modifies:
  - .claude/lessons/feedback_multi_lane_worktree_discipline.md
requires:
  - task: 0
```

**Branch context:**
- `base_branch` = `governance-v0` at trunk tip (verify at task start via `git fetch origin governance-v0 && git log governance-v0..origin/governance-v0`).
- Worker forks `governance-v0` → worktree branch `junior/<auto-generated>`.
- One commit on worker branch; daemon finalize-merges back into `governance-v0` (advisor-laptop manual finalize if DQ #338 daemon-bug recurrence).

**Do NOT** author or modify:
- Any other file under `.claude/lessons/`.
- `.claude/agents/impl-task.md` (that's Task 2).
- `.claude/rules/advisor-orchestrator.md` (that's Task 3).
- Any new lesson file (the change IS the new H2 §; not a separate file).
- Any file under `crates/`, `migrations/`, `tests/`, `docs/brehon-law-inspired-network/**`, `scripts/brehon/`.

**`[P]` cohort discipline:** dispatched in parallel with Task 2 + Task 3. FILES YAML disjoint by construction (different files). No cross-task dependencies.

## 3. Required reading

### 3.0 Plan + governing brief (P0)

1. `.claude/PRPs/plans/v1-retro-followups-r1.plan.md` §1 (Summary), §3 (Problem statement), §10.1 (lesson-extension shape MIRROR), §13 Task 1 (full task body — IMPLEMENT verbatim content, GOTCHA, VALIDATE), §15.1 (DoD grep), §16a Story 1.
2. `.claude/PRPs/reports/session-retro-2026-05-22-v1-dq-schema-r1-cohort-2-ship.md` §"What surprised us" #2 (the mixed-scope retro commit incident) + §"What to change" #1.

### 3.1 MIRROR refs (P0 — read BEFORE first Edit)

3. `.claude/lessons/feedback_multi_lane_worktree_discipline.md` (full file) — current shape. The new H2 § goes at end of file OR before any final "See also" / references §, whichever exists. DO NOT relocate any existing content.
4. `.claude/lessons/feedback_cohort_dq_id_collision.md:1-30` — see how `## Status (post-v1-dq-schema-r1, 2026-05-21)` was added at the top of the file by v1-dq-schema-r1 Task 2 (commit `1803b8546`). Mirror that **content shape** (dated H2 § + lead-in paragraph + citation), NOT the same position (Task 1's H2 goes at END of file, not top).

### 3.2 Lessons that bind §13 decisions

5. `.claude/lessons/feedback_commit_hygiene_lockfiles_and_task_labels.md` — referenced in the new § content. Read so the cross-reference is accurate.
6. `.claude/rules/multi-lane-worktree.md` §"Hard refusals" #6 — the atomic read-mutate-commit protocol the new § EXTENDS. Read to ensure the new § does NOT duplicate that content; it cross-references.
7. `.claude/lessons/feedback_read_canonical_before_writing_spec.md` — canonical-schema-first gate. Read the existing lesson body BEFORE authoring the new §.
8. `.claude/lessons/feedback_junior_pmd_write_convention.md` — LESSON-trailer discipline (encouraged on commit body).

### 3.3 Rules (auto-loaded; spot-check the relevant ones)

9. `.claude/rules/phase-branch.md` — direct-commit policy: v1-retro-followups-r1 ships direct on `governance-v0`. Single commit, no PR.
10. `.claude/rules/decision-queue.md` "Mid-task visibility" — if Junior raises a `kind: blocker` DQ mid-task, commit + push immediately.

## 4. Constraints

1. **New § header verbatim:** `## Pre-commit hygiene in canonical checkout (post-v1-retro-followups-r1, 2026-05-22)` — including the parenthetical date. §15.1 grep depends on this exact match.
2. **Required content keywords (§15.1 grep targets):** the body MUST contain the literal strings `git status --short` and `git diff --cached` (each ≥1 occurrence). Body shape is in plan §13 Task 1 IMPLEMENT; treat it as the contract. Author may improve prose but MUST keep these literals.
3. **EXTENDS Hard Refusal #6, does NOT duplicate.** The parent `multi-lane-worktree.md` §"Hard refusals" #6 already documents the atomic read-mutate-commit protocol. The new § cross-references it (`.claude/rules/multi-lane-worktree.md §"Hard refusals" #6 — atomic read-mutate-commit protocol`) and adds the pre-`git add` step. Authoring duplicate content is a process miss.
4. **Cross-references to `feedback_commit_hygiene_lockfiles_and_task_labels.md`** in the "See also" section of the new § — verify the path exists before writing.
5. **Position: at END of file** (or before final "See also" / references § if such § exists at file tail). Do NOT insert at top; do NOT relocate existing §s.
6. **Worker branch + mid-task push** per `decision-queue.md` "Mid-task visibility": if a blocker DQ is raised, commit + push immediately.
7. **Attribution integrity:** writes ONLY `from: "impl"`. NEVER `answered_by: "advisor"` or `"user"`. Self-resolve with `answered_by: "impl-self-resolved"` ONLY when the answer is in the codebase / plan / lessons.
8. **No cargo invocation.** This sub-phase touches zero Rust. §15 DoD is grep-only.
9. **Commit subject template:** `feat(retro-followups-r1): pre-commit hygiene § in multi-lane lesson (task 1)`. Body cites plan path + Track A scope + Co-Authored-By trailer per `feedback_junior_pmd_write_convention.md`.
10. **Single commit** per `feedback_commit_hygiene_lockfiles_and_task_labels.md`.
11. **No `--no-verify`** — never skip hooks.

### Branch + commit discipline

- Worker branch: `junior/role-impl-task-<auto-id>`. Fork from `governance-v0` HEAD at task-create time.
- Single commit. Daemon finalize-merges back into `governance-v0` (or advisor manual-finalize on daemon-bug recurrence).
- Push immediately after commit so the advisor sees the worker branch on next poll.

### Lesson trailer (encouraged)

If a durable learning surfaces during this task (e.g. a lesson formatting trap, an unexpected grep failure), append a `LESSON: <one-line>` trailer to the commit body per `feedback_junior_pmd_write_convention.md`. The advisor harvests at retro time.

## 5. Validation gates (per plan §15.1 Task 1 + §16a Story 1)

```bash
grep -c '^## Pre-commit hygiene in canonical checkout (post-v1-retro-followups-r1' .claude/lessons/feedback_multi_lane_worktree_discipline.md
# EXPECT: 1

grep -c 'git status --short' .claude/lessons/feedback_multi_lane_worktree_discipline.md
# EXPECT: ≥1

grep -c 'git diff --cached' .claude/lessons/feedback_multi_lane_worktree_discipline.md
# EXPECT: ≥1
```

All three checks MUST pass before the worker commits + pushes.

## 6. Expected output (return to advisor)

```
## Task 1 complete — v1-retro-followups-r1 pre-commit hygiene §

**Branch:** junior/<task-id>
**Commit:** <sha> — feat(retro-followups-r1): pre-commit hygiene § in multi-lane lesson (task 1)
**Files modified:** 1 (.claude/lessons/feedback_multi_lane_worktree_discipline.md)
**§15.1 grep checks:** 3/3 pass
**Pushed:** yes
**Validate-pending DQ:** N/A (no cargo; doc-only)
**LESSON trailer:** <yes/no — and one-line if yes>
```
