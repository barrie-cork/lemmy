# Brief: m3-core-e2e-pilot fix-impl Task-4 `-linux` — pin `livekit-protocol = "=0.7.7"` (dep conflict)

## §1 Role + dispatch line

`[role:impl-task] m3-core-e2e-pilot-fix-task4-pin-livekit-protocol-0.7.7 — see .claude/PRPs/briefs/m3-core-e2e-pilot-fix-impl-4-linux.md`

You are the **impl-task** subagent (Sonnet 4.6). This is a **narrow fix-impl** (≤2 file edits): pin one transitive dependency in `services/bridge/Cargo.toml` and regenerate `Cargo.lock`. NO test-code edit, NO logic change. Runs ALONE (cross-lane cap = 2; you are the only task).

## §2 Scope — the dependency conflict + the fix

**The failure (advisor-diagnosed, user-ratified fix):** Task 4 added `livekit-api = "0.4"` (→ resolved `livekit-api 0.4.24`). `livekit-api 0.4.24` declares `livekit-protocol = "^0.7.7"`, and the Cargo resolver over-selected **`livekit-protocol 0.7.8`** (the newest in-range). `0.7.8` introduced new REQUIRED struct fields (`page_token` on `ListEgressRequest`, `revoke_token_ts` on `RoomParticipantIdentity`, `auth_realm` on `SipInboundTrunkInfo`) that `livekit-api 0.4.24`'s own source does NOT initialize → `4× error[E0063] missing field` **inside the livekit-api crate**, only when the `emergency_mute` test target links it. (`check` + `clippy` pass because they don't compile the test target's full link graph the same way; `test --no-run` does.)

**The fix (user chose "pin livekit-protocol to match"):** force the resolver back to `livekit-protocol 0.7.7` — the version `livekit-api 0.4.24` was actually built against.

**Produces (2 files, ONE commit):**

1. **`services/bridge/Cargo.toml`** — under `[dev-dependencies]` (line ~42, where `livekit-api = "0.4"` lives at line ~49), ADD an explicit pin directly below the `livekit-api` line:
   ```toml
   # Pin: livekit-api 0.4.24 declares livekit-protocol ^0.7.7 but the resolver
   # over-selects 0.7.8, whose new required struct fields (page_token /
   # revoke_token_ts / auth_realm) livekit-api 0.4.24's source does not
   # initialize → E0063 inside livekit-api. Pin to 0.7.7 (the version
   # livekit-api 0.4.24 was built against). Remove if livekit-api is bumped
   # to a release that supports livekit-protocol 0.7.8.
   livekit-protocol = "=0.7.7"
   ```
   - **Use `"=0.7.7"` (exact-equals)** so the resolver cannot drift back to 0.7.8.
   - Place it in `[dev-dependencies]` (it is only needed for the test target — the bridge SRC does not depend on livekit-protocol directly; if a `cargo` resolution error says it must be a normal dep, raise a `kind: blocker` DQ rather than guessing).

2. **`services/bridge/Cargo.lock`** — regenerated by the pin. Do NOT hand-edit; let cargo update it. Since you run NO cargo (NO CARGO ON ELITEDESK), the lockfile regen happens at the laptop validation step — **but** the laptop needs the Cargo.toml change to regenerate. So: make ONLY the Cargo.toml edit, commit, push, write the DQ, STOP. The advisor (laptop) will run `cargo-linux.sh` which regenerates Cargo.lock as part of the gate; if the gate shows an uncommitted Cargo.lock delta, the advisor commits it. **Do NOT attempt to regenerate Cargo.lock yourself.**

**Do NOT:**
- Touch `services/bridge/tests/emergency_mute.rs` (the test code is CORRECT — the fault was the transitive dep version, not the test).
- Touch any other file, crate, migration, const, registry, or plan.
- Bump `livekit-api` (user explicitly chose the pin-protocol path, NOT the bump-api path).
- Run cargo (laptop runs all cargo).

**Branch:** forks from `phase-m3-core-e2e-pilot` (current tip ≥ `148556c30`; confirm at task spawn).

## §3 Required reading

- **This brief** (the diagnosis is complete; you are executing the pin).
- **`services/bridge/Cargo.toml`** — the WHOLE `[dev-dependencies]` section; place the pin adjacent to `livekit-api = "0.4"`.
- **Lessons (§2.4 file-class injection — Cargo.toml dep edit):**
  - `pattern_cargo_feature_flag_propagation.md` — exact-version pins + lockfile regen behaviour.
  - `feedback_validate_pending_laptop_write_then_stop.md` — write the DQ and STOP; the laptop runs all cargo (incl. the Cargo.lock regen).
  - `feedback_bridge_validates_on_linux_not_windows.md` — bridge cargo is Linux-only via cargo-linux.sh; you run none.

## §4 Constraints

- **Exact pin only (`=0.7.7`):** do NOT use `"0.7.7"` (caret-implied) — that still allows 0.7.8. Use `"=0.7.7"`.
- **Dev-dep placement:** the pin is a `[dev-dependencies]` entry (test-target only). If cargo rejects it there (must be a normal dep for the lock to take effect), raise a `kind: blocker` DQ — do NOT move it to `[dependencies]` on a guess (that would make livekit-protocol a runtime dep of the bridge, a scope change).
- **ONE commit** — `fix(rtc): pin livekit-protocol =0.7.7 to match livekit-api 0.4.24 (E0063 dep conflict, task 4 -linux)`. `LESSON:` trailer: `LESSON: livekit-api 0.4.24 declares livekit-protocol ^0.7.7 but 0.7.8 adds required fields it doesn't init — pin =0.7.7.`
- **NO daemon cargo.** Write ONE DQ (`bash scripts/brehon/dq-v3-new-entry.sh` for the id, `bash scripts/brehon/dq-v3-append-fragment.sh <frag>.json --pending`):
  ```
  kind: "validate-pending-laptop-linux"
  branch: "phase-m3-core-e2e-pilot"
  phase_task: 4
  commands: [
    "scripts/brehon/cargo-linux.sh check --manifest-path services/bridge/Cargo.toml",
    "scripts/brehon/cargo-linux.sh clippy --manifest-path services/bridge/Cargo.toml --no-deps -- -D warnings",
    "scripts/brehon/cargo-linux.sh test --manifest-path services/bridge/Cargo.toml --test emergency_mute --no-run"
  ]
  result: null
  log_slice: null
  failed_commands: null
  ```
  Commit + push on the worker branch, then **STOP**.
- **No stray temp artifacts:** delete any DQ fragment file after appending (do NOT commit it).
- **DQ mid-task discipline:** any blocker → `kind: "blocker"` DQ, commit + push, stop. Do NOT guess.
- **Handover:** inline (no handover file).

## §5 HANDOVER (worker fills at task end — return inline)

```yaml
HANDOVER:
  task: m3-core-e2e-pilot-fix-task4-pin-livekit-protocol
  filesModified: [services/bridge/Cargo.toml]   # + Cargo.lock if you were able to regen; else laptop does it
  keyDecisions:
    - "Pinned livekit-protocol = \"=0.7.7\" in [dev-dependencies] (livekit-api 0.4.24 requires ^0.7.7; resolver over-selected 0.7.8 whose new required fields E0063'd inside livekit-api)"
    - "Did NOT touch tests/emergency_mute.rs (test code correct; fault was transitive dep version)"
    - "Did NOT bump livekit-api (user chose pin-protocol path)"
  validate_dq_linux: <id>
  notes: "<confirm pin is =0.7.7 exact; confirm in [dev-dependencies]; confirm no test-code edit; confirm Cargo.lock left for laptop regen OR regenerated if possible; confirm no stray dq-frag committed>"
```
