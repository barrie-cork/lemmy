# v1-retro-followups-r1 — impl Task 3 brief (Cohort 1 [P], polling-loop handover discipline)

## 1. Role + dispatch line

`[role:impl-task] v1-retro-followups-r1 Task 3 — pre-compact handover bullet in advisor-orchestrator §1 — see .claude/PRPs/briefs/v1-retro-followups-r1-impl-3.md`

## 2. Scope

Implement plan §13 **Task 3** of `.claude/PRPs/plans/v1-retro-followups-r1.plan.md`. One commit, one file modified:

```yaml
creates: []
modifies:
  - .claude/rules/advisor-orchestrator.md
requires:
  - task: 0
```

**Branch context:**
- `base_branch` = `governance-v0` at trunk tip.
- Worker forks `governance-v0` → worktree branch `junior/<auto-generated>`.
- One commit; daemon finalize-merges (or advisor manual-finalize on DQ #338 recurrence).

**Do NOT** author or modify:
- Any §section in `advisor-orchestrator.md` other than §1 "Polling loop".
- `.claude/lessons/feedback_multi_lane_worktree_discipline.md` (Task 1).
- `.claude/agents/impl-task.md` (Task 2).
- Any other `.claude/rules/*.md` file.
- Any file under `crates/`, `migrations/`, `tests/`, `docs/brehon-law-inspired-network/**`, `scripts/brehon/`.

**`[P]` cohort discipline:** dispatched in parallel with Task 1 + Task 2. FILES YAML disjoint.

## 3. Required reading

### 3.0 Plan + governing brief (P0)

1. `.claude/PRPs/plans/v1-retro-followups-r1.plan.md` §1, §3, §10.3 (rule-section-extension shape MIRROR), §13 Task 3 (full task body — verbatim bullet content), §15.1 (DoD), §16a Story 3.
2. `.claude/PRPs/reports/session-retro-2026-05-22-v1-dq-schema-r1-cohort-2-ship.md` §"What surprised us" #3 (the resume-after-compact validation) + §"What to carry forward" (pre-compact handover discipline).

### 3.1 MIRROR refs (P0 — read BEFORE first Edit)

3. `.claude/rules/advisor-orchestrator.md` §1 "Polling loop" (the bullet list at top of file, between `## 1. Polling loop` heading and `## 2. Brief authoring` heading) — current shape. The new bullet goes at the END of that bullet list, AFTER the most-recent existing bullet (the SessionStart multi-lane check bullet added 2026-05-22 by the concurrent session). Do NOT relocate any existing bullet.
4. `.claude/rules/advisor-orchestrator.md` §1 — re-read the two bullets just before the insertion point (SessionStart canonical-PMD guard + SessionStart multi-lane check) so the new bullet's prose tone matches.
5. `.claude/PRPs/handovers/v1-dq-schema-r1-cohort-2-handover-2026-05-22.md` — the canonical handover-file example to cite in the new bullet's body.

### 3.2 Lessons

6. `.claude/lessons/feedback_advisor_impl_communication_via_handover_files.md` — the canonical lesson on handover files. The new bullet refers to this discipline.
7. `.claude/lessons/feedback_principles_not_rules.md` — prose tone.
8. `.claude/lessons/feedback_junior_pmd_write_convention.md` — LESSON trailer discipline.

### 3.3 Rules

9. `.claude/rules/phase-branch.md` — direct-commit policy.
10. `.claude/rules/decision-queue.md` "Mid-task visibility" — push discipline.

## 4. Constraints

1. **New bullet lead-in verbatim:** `**Pre-compact handover discipline (post-v1-retro-followups-r1, 2026-05-22):**` — including bold markers and parenthetical date. §15.1 grep depends on this exact prefix.
2. **Required content (§15.1 grep targets):** the new bullet body MUST contain the literal strings `.claude/PRPs/handovers/` (the path convention) and a reference to `Cohort 2 handover` (the v1-dq-schema-r1 Cohort 2 handover file as validating evidence). Each ≥1 occurrence.
3. **Bullet body shape:** plan §13 Task 3 IMPLEMENT provides the verbatim body. Treat as contract. Author may improve prose but MUST keep the lead-in prefix + the two required keyword strings + the citation of the validating handover file.
4. **Position: END of §1 "Polling loop" bullet list.** The existing list is ordered (CWD check → cadence → status routing → DQ patterns → SessionStart hooks). The new bullet goes AFTER the most-recent existing bullet (SessionStart multi-lane check, added 2026-05-22). Do NOT insert at top; do NOT split adjacent bullets.
5. **Concurrent-session safety:** another advisor session may have added a bullet to §1 between plan-author time (2026-05-22) and worker dispatch. Verify the file's §1 has not gained new bullets BEYOND the SessionStart multi-lane check; if it has, append AFTER them (still last). Do NOT remove a bullet a concurrent session authored.
6. **Worker branch + mid-task push** per `decision-queue.md` "Mid-task visibility".
7. **Attribution integrity:** `from: "impl"` only. NEVER `answered_by: "advisor"` / `"user"`.
8. **No cargo.**
9. **Commit subject template:** `feat(retro-followups-r1): pre-compact handover bullet in advisor-orchestrator §1 (task 3)`.
10. **Single commit.**
11. **No `--no-verify`.**

### Branch + commit discipline

Standard: worker branch `junior/role-impl-task-<auto-id>`, single commit, push immediately, daemon finalize-merges back.

### Lesson trailer (encouraged)

Append `LESSON: <one-line>` trailer if a durable learning surfaces.

## 5. Validation gates (per plan §15.1 Task 3 + §16a Story 3)

```bash
grep -c 'Pre-compact handover discipline (post-v1-retro-followups-r1' .claude/rules/advisor-orchestrator.md
# EXPECT: ≥1

grep -c '\.claude/PRPs/handovers/' .claude/rules/advisor-orchestrator.md
# EXPECT: ≥1

grep -c 'Cohort 2 handover' .claude/rules/advisor-orchestrator.md
# EXPECT: ≥1
```

All three checks MUST pass before commit + push.

## 6. Expected output (return to advisor)

```
## Task 3 complete — v1-retro-followups-r1 pre-compact handover bullet

**Branch:** junior/<task-id>
**Commit:** <sha> — feat(retro-followups-r1): pre-compact handover bullet in advisor-orchestrator §1 (task 3)
**Files modified:** 1 (.claude/rules/advisor-orchestrator.md)
**§15.1 grep checks:** 3/3 pass
**Pushed:** yes
**Validate-pending DQ:** N/A (no cargo)
**LESSON trailer:** <yes/no>
```
