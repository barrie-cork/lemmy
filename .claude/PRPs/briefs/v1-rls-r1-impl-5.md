# v1-rls-r1 — impl Task 5 brief (serial chain)

## 1. Role + dispatch line

`[role:impl-task] v1-rls-r1 Task 5 — .gitignore RLS runtime journals — see .claude/PRPs/briefs/v1-rls-r1-impl-5.md`

## 2. Scope

Implement plan §13 **Task 5** (lines 1523-1586) of `.claude/PRPs/plans/v1-rls-r1.plan.md` (committed `51fd76f00`). One commit, one file modified:

```yaml
creates: []
modifies:
  - .gitignore   # add .claude/harvest/ and .claude/governance-log/ lines
requires:
  - task: 4
    reason: "Task 4 establishes the .claude/harvest/<iso-week>.md output convention; this gitignore entry references it."
```

Add two lines under the existing `.claude/`-runtime block (adjacent to `.claude/auto-state/` at gitignore lines 69-71). Both are runtime-journal outputs per PRECON-9 lean:

```
# RLS runtime journals — see .claude/PRPs/plans/v1-rls-r1.plan.md §11
.claude/harvest/
.claude/governance-log/
```

**Do NOT** author:
- Any other artifact (Tasks 6, 8, 9, 10, 11, 12).
- New runtime data — Tasks 4 and 7 produce the writers; this task only protects their outputs from accidental commit.
- Any edit under `crates/`, `migrations/`, `tests/`, `docs/brehon-law-inspired-network/99-decisions-and-open-questions.md`, `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`.

**Branch context:**
- `base_branch` of this Junior task = `phase-v1-rls-r1` (at `51fd76f00`).
- Worker forks from `phase-v1-rls-r1` to its own worktree branch `junior/<auto-generated>`.
- One commit on the worker branch; daemon finalize-merges back into `phase-v1-rls-r1`.

## 3. Required reading

### 3.0 Plan + governing brief (P0)

1. `.claude/PRPs/plans/v1-rls-r1.plan.md` §13 Task 5 (lines 1523-1586 — IMPLEMENT spec, MIRROR, GOTCHAs, VALIDATE).
2. `.claude/PRPs/briefs/v1-rls-r1-planning-1.md` §0.1 PRECON-9 (runtime journals).

### 3.1 Existing artifact MIRROR (P0)

3. `.gitignore` lines 43-73 — existing `.claude/` runtime block (mirror style).

### 3.2 Cross-cohort DQ id collision context (P0 — read so DQ raise doesn't repeat the bug)

4. `.claude/lessons/feedback_cohort_dq_id_collision.md` — Cohort A produced 5 DQs all with id=307; serial worker (this task) must compute next_id correctly against current phase tip (max id is 311 → next_id is 312). Per the canonical recipe in `.claude/refs/dq-recipes.md` §"Recipe 1".

### 3.3 Rules (auto-loaded)

5. `.claude/rules/decision-queue.md` — schema v2; impl-task writes `from: "impl"`; cross-archive next_id.
6. `.claude/rules/phase-branch.md` — worker → daemon-finalize → phase branch.
7. `.claude/rules/branch-manager.md` — file ownership.

## 4. Constraints

1. **Exactly two new `.gitignore` lines + one comment header.** No third pattern.
2. **Insertion at end of existing `.claude/`-runtime block** (between line 71 `.claude/auto-state/` and line 73 `.claude/skills/*-workspace/`). Comment header `# RLS runtime journals — see .claude/PRPs/plans/v1-rls-r1.plan.md §11`.
3. **Worker branch + mid-task push discipline** per `decision-queue.md` "Mid-task visibility".
4. **Attribution integrity**: `from: "impl"` only.
5. **No cargo invocation in this commit's content** — Watchpoint #3.
6. **DQ id discipline (CRITICAL — read lesson #4 above):** compute `next_id` from the WORKER BRANCH's view of `.claude/decision-queue.json` after rebasing from the latest phase tip. Current max DQ id at phase tip `51fd76f00` is 311 → your next_id is 312. Single-task serial cohort = no cross-worker collision risk if you compute from the fetched tip.
7. **§16a Story 3 checkpoint** — VALIDATE step's grep + check-ignore + cargo-check all exit 0.
8. **Pre-push cargo discipline per `feedback_fix_impl_pre_push_cargo_check.md`** — run VALIDATE locally before push.
9. **Commit subject template** — `feat(rls-r1): add .claude/harvest/ + .claude/governance-log/ to .gitignore (task 5)`. Body cites plan path + commit + Track B scope.
10. **Single commit** per plan §13 norm.
11. **No `--no-verify`** — never skip hooks.

## 5. Validation gate (DoD per plan §15)

### 5.1 Output shape (per plan §13 Task 5 VALIDATE block)

```bash
grep -E "^\.claude/(harvest|governance-log)/$" .gitignore | wc -l   # EXPECT 2
git check-ignore -v .claude/harvest/foo.md 2>&1                     # EXPECT matches
git check-ignore -v .claude/governance-log/retro-bypass.jsonl 2>&1  # EXPECT matches
```

### 5.2 Static analysis (laptop-mode per PRECON-7)

```bash
bash scripts/brehon/cargo-check.sh --workspace --features full > .claude/PRPs/debug/v1-rls-r1-task5-check.log 2>&1
echo "exit: $?"
tail -20 .claude/PRPs/debug/v1-rls-r1-task5-check.log
# EXPECT: exit 0
```

### 5.3 §15 validate-pending-laptop DQ (raise after worker push)

Per advisor-orchestrator §5.2: raise the DQ entry with `kind: "validate-pending-laptop"`, `from: "impl"`, `branch: "<worker-branch>"`, `phase_task: 5`, `commands: ["bash scripts/brehon/cargo-check.sh --workspace --features full > .claude/PRPs/debug/v1-rls-r1-task5-check.log 2>&1; echo exit: $?"]`, `id: 312` (per Constraint 6), `result: null`, `log_slice: null`, `failed_commands: null`, `answer: null`, `answered_by: null`, `resolved_at: null`. Commit + push BEFORE worker exit.

## 6. KNOWN harness limitations

1. **Claude Code sensitive-file gate** — `.gitignore` is at repo root, not under `.claude/**`; should not trigger the gate. If unexpectedly blocked, fall back to `/tmp/gitignore.patch` + manual append.
2. **Daemon finalize-merge** — operates correctly per `feedback_junior_finalize_stage_per_job.md`.

## 7. Next steps after this task

- Daemon finalize-merge brings Task 5 commit onto `phase-v1-rls-r1`.
- Advisor polling loop runs cargo-check + mutates DQ to `result: "pass"`.
- Task 6 (lesson) advances next.

## 8. Commit subject template (verbatim)

```
feat(rls-r1): add .claude/harvest/ + .claude/governance-log/ to .gitignore (task 5)
```

Commit body MUST cite:
- Plan path + SHA: `.claude/PRPs/plans/v1-rls-r1.plan.md (51fd76f00)`.
- §13 Task 5 implemented.
- PRECON-9 honoured (runtime journals gitignored).
- Co-Authored-By trailer.

## 9. DoD for this brief

- [x] Brief committed on `phase-v1-rls-r1` BEFORE Junior task dispatched.
- [x] Brief specifies single-file output + serial chain status.
- [x] Brief cites plan §13 Task 5 by line range (1523-1586).
- [x] Brief lists Required reading per §3 including the DQ id-collision lesson.
- [x] Brief §4 enumerates 11 hard constraints with explicit next_id=312 guidance.
- [x] Brief §5 names the validate-pending-laptop DQ shape verbatim.
- [x] Brief §6 carries the KNOWN harness limitation block.

## 10. Commit subject for this brief

`chore(advisor): author Task 5 brief for v1-rls-r1 (serial chain)`
