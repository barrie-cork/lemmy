# Ephemeral build and audit logs
*.log
build-*.log
audit-*.log

# Pre-phase wrapper-audit completion flags (local cargo cache state, per-dev)
audit-*-complete.flag

# Sensitive token stash (defense-in-depth — primary location is outside repo)
brehon-telegram-token.txt
