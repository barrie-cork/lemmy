# HANDOVER — comms-research reconcile complete, 2026-06-28

Read this first. It supersedes `NEXT-SESSION.md` for the *current* state (that file
described the pre-reconcile starting point). Zero conversation context needed.

## Where we are: the research reconcile is DONE

The Perplexity Deep Research output (`research-prompts/perplexity-results-2026-06-27.md`,
372KB) has been fully digested and reconciled into the themes, **under the
founder's personal direction** (the founder steered every high-level framing call;
the agent executed). All 16 themes + `machine/messaging.yaml` are reconciled with
the research AND honesty-checked against the live v0 code.

**Working method that was locked (binding, in `research-prompts/founder-standout-points.md`):**
1. Re-express every research finding to **what the Brehon Consensus layer brings,
   in our own voice.** Research labels (e.g. "accountability without annihilation")
   are *internal strategy only — never public copy.*
2. Themes are written in **audience-neutral mechanics**; per-group spins (chess
   club / gardeners / neighbours / activists) live in `drafts/` only.
3. Never use the phrase "cancel culture" in lead copy.

## Key decisions the founder made (now in the files)

- **Spine = bottom-up (HOW) + "it's a choice, not a natural law" (WHY)**, unified
  in `themes/14`. Public WHY line: *"You don't need a tech company to run your
  community fairly. You never did."* ("ideology" framing kept to deep copy.)
- **Greeting verb softened** "govern yourselves" → **"run your group / your own
  way."** "Govern" only appears one level down (press/about). (`themes/02`, `08`)
- **"Community jury" promoted to primary greeting noun** (CHI evidence: jury tests
  fairer than moderation across the spectrum), with the reframe *verdict = fair
  peer decision, not courtroom.* New hero shape: **"Run your group your own way —
  with a community jury, not a boss."** (`themes/02`)
- **Comparison table** (Mastodon/Loomio/Platform-Coop/RJC/Fediverse) added to
  `themes/02`.

## Load-bearing honesty results (verified against live code 2026-06-27)

These were the highest-value catches — present-tense claims now all match shipped code:

- **Court-case claim:** NO Brehon-specific modern case exists. Cite Mabo / Ellis /
  Tsilhqot'in as *customary-law* precedents only (all First Nations, none Irish).
  The Brehon-specific claim is OUT. (`themes/03`, `messaging.yaml` proof_points)
- **Reputation DECAY:** wired but **OFF by default** (`feature.reputation_v1_decay_enabled`=false).
  Say "can be enabled / designed to fade", NEVER "standing fades". Fixed in
  themes 04, 06, 10, 15 + yaml.
- **LIVE in v0** (safe present-tense): random + diversity-aware jury draw
  (`ORDER BY random()` + 3-phase anti-clique filter), juror cooldown (7-day
  default), reporter-reward (+10 upheld / −5 dismissed), vouching/endorsement,
  time-based/provisional entry, and the per-community config knobs (deltas,
  thresholds, panel size, quorum). 7 of 8 `themes/15` knobs are LIVE.

## Commit trail (all on governance-v0, direct — docs meta-work, no PR)

`3ce6f3658` digests · `3eb1282f3` spine merge · `efdd9674f` repair angle + method
rule · `8bb026394` reputation theme · `15963cc3f` traps/pre-emption · `420e6a618`
naming · `63abdeb08` ladder propagation · `8fbc1667e` honesty flags 11/12/13/15 ·
`c38ea2d9a` analogies/audience/theme-16 (final).

## What remains — TWO things

### 1. One deferred founder fork (small)
**Which positioning angle LEADS the pamphlet** was deferred (not blocking the
reconcile). Options recorded in `themes/16` "Positioning angles": Angle 1
(fair-process/repair — research's evidence-backed top pick), Angle 2 (rules you
wrote / consent), Angle 3 (old-idea — use with care). The bottom-up spine frames
all three. **Surface this fork to the founder when drafting begins.** Also still
open: how explicit to make the "ideology" framing in deep copy (`themes/14` tone
caution).

### 2. The actual writing (the gate the founder set)
Per `NEXT-SESSION.md` Step 3: **the founder confirms before any drafting.** The
first deliverable is ONE pamphlet (rung 3 in `themes/06-message-ladder.md`),
written to grassroots organisers, bottom-up spine, analogy-led, honest about alpha
status. Drafts go under `drafts/` (create it). **Do not start drafting unprompted
— surface readiness and wait for the founder.**

## Notes for the next session

- **Foreign WIP present:** untracked files under `docs/research/`
  (`brehon-app-plane-feasibility-*.md`, `brehon-rulemaking-uiux-recommendations.md`)
  appeared mid-session — NOT part of this work, another process/session created
  them. Left untouched. Don't commit them with comms-research work.
- **A/B testing flagged twice** (the category noun, the lead analogies) — real
  testing on actual organisers is recommended before print, per the research
  (first label dominates comprehension permanently).
- This is `governance-v0` docs meta-work: direct commits, small units, no PR
  (per `.claude/rules/phase-branch.md`).
